{
  "name": "@crucible/backend",
  "version": "0.0.0",
  "private": true,
  "description": "Crucible backend package.",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "tsc -p tsconfig.json",
    "check-types": "tsc --noEmit",
    "test": "bun test",
    "lint": "eslint \"src/**/*.ts\" --max-warnings=0"
  },
  "dependencies": {
    "hono": "^4.12.15"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.3"
  }
}
